{
    "version": 2,
    "buildCommand": "npm run build",
    "installCommand": "npm ci",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}